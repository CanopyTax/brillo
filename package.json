{
  "name": "brillo",
  "version": "0.0.1",
  "license": "MIT",
  "repository": "github:geoctrl/brillo",
  "scripts": {
    "build": "yarn main && yarn renderer",
    "main": "rollup src/main.js --config rollup.config.js --file main.js",
    "renderer": "rollup src/renderer.js --config rollup.config.js --file renderer.js",
    "test": "jest",
    "test-watch": "jest --watch"
  },
  "dependencies": {},
  "peerDependencies": {
    "electron": ">=6",
    "rxjs": ">=6"
  },
  "devDependencies": {
    "electron": "^7.1.9",
    "jest": "^25.1.0",
    "rollup": "^2.0.6",
    "rxjs": "^6.5.4"
  }
}
