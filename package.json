{
  "name": "brillo",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "repository": "github:geoctrl/brillo",
  "scripts": {
    "build": "yarn build-main && yarn build-renderer",
    "build-main": "rollup src/main.js --file main.js",
    "build-renderer": "rollup src/renderer.js --file renderer.js",
    "test": "jest",
    "test-watch": "jest --watch"
  },
  "dependencies": {},
  "peerDependencies": {
    "electron": ">=6",
    "rxjs": ">=6"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.2",
    "electron": "^7.1.9",
    "jest": "^25.1.0",
    "rollup": "^2.0.6",
    "rxjs": "^6.5.4"
  }
}
